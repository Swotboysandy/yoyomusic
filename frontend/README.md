# YoYoMusic Frontend (Next.js)

## Setup
1. Install dependencies:
   ```bash
   npm install
   ```

## Configuration
Copy `.env.local.example` to `.env.local` (or create it).
Required variables:
- `NEXT_PUBLIC_API_URL`

## Running Locally
```bash
npm run dev
```

## Build
```bash
npm run build
```
